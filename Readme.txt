1. Open the cmd in both client folder and server folder seprately.
2. Or cd C:/Desktop/ServerGuy/client and C:/Desktop/ServerGuy/server
3. Install the dependencises. Run : npm i in both cmd.
4. build run in both cmd:
npm start

Note: The react app will open at localhost:3000 probably and api at port 8000